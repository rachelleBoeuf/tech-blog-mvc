<div class="col-sm-12" style="height: 100%">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse">
            <ul>
                <li class="nav-item active">
                    <a class="nav-link btn btn-primary" href="/">Home</a>
                </li>
                {{#if user.logged_in}}
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="Dashboard">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="/Logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link btn btn-primary" href="/Login">Login</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </nav>
</div>